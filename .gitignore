# Originální podklady – pouze lokálně, nepushovat (1.4 GB)
PODKLADY/

# Wrangler lokální cache/build (Cloudflare Worker deploy)
.wrangler/

# Node závislosti (qrcode-generator pro Worker)
node_modules/
